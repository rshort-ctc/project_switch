[package]
name = "switch-desktop"
version = "0.1.0"
description = "SWITCH local desktop shell"
authors = ["City Tele Coin"]
edition = "2021"

[lib]
name = "switch_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
rfd = "0.15"
tauri = { version = "2", features = [] }
